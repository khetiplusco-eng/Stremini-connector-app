name: stremini_app
description: Stremini AI Mobile-first Assistant Platform
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^13.2.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  http: ^1.2.1
  flutter_svg: ^2.0.10
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  # Add material icons
  assets:
    # - assets/images/
